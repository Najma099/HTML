// normal function 

console.log(one(7));
function one(num) {
    return num + 1;
};


const arr = function (numm ) {
    return numm + 2;
};

// we need to instialise the variable first
console.log(arr(5));


const brr = (numm) => {
    return (numm * numm)
};
console.log(brr(7));

